import React from 'react'

const LanguageTile = ( { name, logo } ) => {
    return (
        <div>
            <div>{ name }</div>
            <div>{ logo }</div>
        </div>
    )
}

export default LanguageTile
